import { } from 'mocha';
import { expect } from 'chai';
import * as sinon from 'sinon';

import { Promise } from "../../src/util/promise";
import * as promise from "../../src/util/promise";

describe("promise", () => {

    // TODO
    // it('should be autodetectable', () => {
    //     const target = Function("return this")().Promise;
    //     t.truthy(Promise);
    //     t.is(Promise, target);
    //     t.is(promise.getPromiseContructor(), target);
    // });

    // TODO
    // it('constructor should be settable', () => {
    //     const something: any = {};
    //     promise.setPromiseContructor(something);
    //     expect(Promise).same(something)
    //     expect(promise.getPromiseContructor()).same(something)
    // });
});
